// Shows the default html page once the extension is installed
// I.e in the future this can just direct us to a website, for 
// now it isn't useful
// chrome.runtime.onInstalled.addListener(() => {
//   chrome.tabs.create({
//     url: ''
//   })
// })


// function openApp() {
//   chrome.app.window.create({url: './popups/default-popup.html'})
// }

// chrome.action.onClicked.addListener(openApp);